import React from 'react';
import {Text} from 'react-native';

export interface IBooksOption {
  id: number;
  description: string;
  title: string;
}

export const BooksScreen = () => {
  return <Text>Books Screen</Text>;
};
